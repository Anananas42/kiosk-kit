{
  "name": "kioskkit",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client \"pnpm --filter @kioskkit/server dev\" \"pnpm --filter @kioskkit/client dev\"",
    "build": "pnpm --filter @kioskkit/shared build && pnpm --filter @kioskkit/server build && pnpm --filter @kioskkit/client build",
    "start": "node packages/server/dist/index.js",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3",
    "vitest": "^4.1.0"
  }
}
